generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  USER
  ADMIN
}

enum AppCode {
  PROJECTS
  TASKS
  CALENDAR
  BAMBOO
  WORKOUT
}

enum ProjectPriority {
  LOW
  MEDIUM
  HIGH
}
