generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  USER
  ADMIN
}

enum AppCode {
  PROJECTS
  TASKS
  CALENDAR
  BAMBOO
  WORKOUT
  SMOOTHIE
  INVOICE
  SKATING_BIBLE
}

enum ProjectPriority {
  LOW
  MEDIUM
  HIGH
}
